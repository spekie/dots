#!/bin/sh
printf "bitrate (e.g. 8k, 64k, 128k, 320k): "
read bitrate

for file in *.flac *.wav; do
  [ -e "$file" ] || continue

  base="${file%.*}"
  output="${base}.mp3"

  ffmpeg -i "$file" \
    -b:a $bitrate \
    "$output"
done
